# Docker Assets

This directory is reserved for environment-specific container assets that support the enterprise deployment model.

- `nginx/` holds reverse-proxy configuration.
- `kubernetes/` holds orchestration manifests.
